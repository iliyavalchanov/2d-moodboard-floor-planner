/** Pixels per meter — 50 canvas px = 1 m */
export const PIXELS_PER_METER = 50;

/** Grid dot spacing in pixels */
export const GRID_SIZE = 25;

/** Snap threshold in pixels */
export const SNAP_THRESHOLD = 10;

/** Snap-to-wall threshold for fixtures */
export const WALL_SNAP_THRESHOLD = 20;

/** Zoom limits */
export const MIN_SCALE = 0.1;
export const MAX_SCALE = 5;

/** Zoom step multiplier per scroll tick */
export const ZOOM_STEP = 1.1;

/** Stage default dimensions */
export const DEFAULT_STAGE_WIDTH = 1200;
export const DEFAULT_STAGE_HEIGHT = 800;
